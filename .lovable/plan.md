I’ll fix the featured film text cropping without changing the overall design.

Plan:
1. Update the desktop info panel so the title and synopsis are not clipped by fixed-height containers.
2. Replace the current hard cropping behavior with responsive text sizing/line-height that gives Persian and English enough vertical room.
3. Keep the Watch button visually pinned and stable across slides, but let the text area above it breathe so long Persian titles/synopses show cleanly.
4. Check the mobile layout too: keep controls centered, prevent Persian text from being cut, and avoid adding extra empty space.
5. Verify the homepage hero after changes at desktop and mobile sizes so title, synopsis, and button are fully visible in both languages.