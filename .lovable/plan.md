## Eyebrow label size increase

Increase the eyebrow section labels in `src/components/films-row.tsx` from `text-[10px]` to `text-[14px]` (or `text-sm`). The labels are the amber "Iranian Originals" / "New Releases" eyebrows above each film rail.

This is a single CSS class change in one component.