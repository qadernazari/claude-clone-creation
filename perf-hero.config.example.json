{
  "$comment": "Copy to perf-hero.config.json (or point PERF_HERO_CONFIG at it) to override LCP budgets and preload gate expectations without editing scripts/perf-hero-check.mjs. Env vars still win (LCP_BUDGET_MS_DESKTOP, EXPECT_BUCKET_DESKTOP, FORBID_BUCKET_DESKTOP, HERO_SELECTOR_DESKTOP, and matching *_MOBILE / *_TABLET / *_LAPTOP variants).",
  "lcp_budget_ms": 2000,
  "viewports": {
    "mobile": {
      "lcp_budget_ms": 1800,
      "expect_bucket": "film-covers",
      "forbid_bucket": "film-thumbnails"
    },
    "tablet": {
      "lcp_budget_ms": 2800,
      "expect_bucket": "film-thumbnails",
      "forbid_bucket": "film-covers"
    },
    "laptop": {
      "lcp_budget_ms": 1600,
      "expect_bucket": "film-thumbnails",
      "forbid_bucket": "film-covers"
    },
    "desktop": {
      "lcp_budget_ms": 1400,
      "expect_bucket": "film-thumbnails",
      "forbid_bucket": "film-covers"
    }
  }
}
